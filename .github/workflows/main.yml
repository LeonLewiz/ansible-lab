name: CI

on:
  push:
    branches:
      - main

jobs:
  sync-repo:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy all files to ansible-lab
        run: |
          cp -rT . ~/ansible-lab/
